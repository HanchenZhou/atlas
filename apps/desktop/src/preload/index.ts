// Renderer talks to the daemon directly over HTTP/SSE on localhost — nothing to
// expose via contextBridge yet. Keep this file as a placeholder so Electron's
// preload entry exists and can be filled later if we need IPC.
export {};
