{
  "name": "atlas",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.8",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "dev:daemon": "turbo run dev --filter=@atlas/daemon",
    "build": "turbo run build",
    "test": "turbo run test",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  }
}
